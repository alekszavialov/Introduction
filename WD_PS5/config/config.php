<?php

define('ERROR', 'Oops, smth go wrong(');

define('MIN_NAME_LENGTH', 4);

define('MAX_NAME_LENGTH', 20);

define('MIN_PASS_LENGTH', 6);

define('MAX_PASS_LENGTH', 16);

define('LOGIN_REG', '([^\w\d-_])');

define('PASSWORD_REG', '([^\w\d])');
